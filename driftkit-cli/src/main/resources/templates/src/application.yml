spring:
  application:
    name: /*PROJECT_NAME*/

driftkit:
  clients:
    openai:
      api-key: ${OPENAI_API_KEY}
      model: gpt-4
      temperature: 0.7

logging:
  level:
    ai.driftkit: INFO
    com.example: DEBUG